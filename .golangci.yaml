version: "2"

linters:
  settings:
    errcheck:
      # Writing to stdout/stderr in a CLI tool cannot meaningfully fail.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
